import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.rscorp.kiasujobs",
  appName: "KiasuJobs",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
  backgroundColor: "#070A11",
};

export default config;
